interface:
  display_name: "Desktop"
  short_description: "Repo-specific patterns and workflows for desktop"
  default_prompt: "Use the desktop repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true